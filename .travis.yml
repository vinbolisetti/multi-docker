sudo: required
language: generic

services:
  - docker

before_install:
  - docker build -t vinbolisetti/react-test -f ./client/Dockerfile.dev ./client

script:
  - docker run -e CI=true vinbolisetti/react-test npm test

after_success:
  - docker build -t vinbolisetti/multi-client ./client
  - docker build -t vinbolisetti/multi-nginx ./nginx
  - docker build -t vinbolisetti/multi-server ./server
  - docker build -t vinbolisetti/multi-worker ./worker
  # Log in to the docker CLI
  - echo "$DOCKER_PASSWORD" | docker login -u "$DOCKER_ID" --password-stdin
  # Take those images and push them to docker hub
  - docker push vinbolisetti/multi-client
  - docker push vinbolisetti/multi-nginx
  - docker push vinbolisetti/multi-server
  - docker push vinbolisetti/multi-worker
